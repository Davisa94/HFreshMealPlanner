# Now that we have the data in the right format with coordinates attached to the words, we can sort the data

import pandas as pd
import numpy as np

# Got the output image visualization working, now we need to group groups of text blocks within a set region as steps.
# Expected region for ingredients is 